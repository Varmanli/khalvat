"use client"; import { useRouter } from "next/navigation"; import { Check } from "lucide-react";
export function MilestoneToggle({goalId,id,done}:{goalId:string;id:string;done:boolean}){const router=useRouter();return <button onClick={async()=>{await fetch(`/api/goals/${goalId}/milestones/${id}/toggle`,{method:'POST'});router.refresh()}} className={done?"flex size-7 shrink-0 items-center justify-center rounded-full bg-primary text-white":"flex size-7 shrink-0 items-center justify-center rounded-full border-2 border-border text-transparent hover:border-primary"}><Check className="size-4"/></button>}
