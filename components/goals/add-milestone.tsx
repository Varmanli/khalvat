"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { Plus } from "lucide-react";
export function AddMilestone({goalId}:{goalId:string}){const [title,setTitle]=useState(''),router=useRouter();return <form onSubmit={async e=>{e.preventDefault();if(!title.trim())return;await fetch(`/api/goals/${goalId}/milestones`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title})});setTitle('');router.refresh()}} className="mt-3 flex gap-2"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="یک مرحله تازه..." className="min-w-0 flex-1 rounded-xl border border-border bg-background px-3 py-2 text-xs outline-none focus:border-primary"/><button className="inline-flex items-center gap-1 rounded-xl bg-primary px-3 py-2 text-xs font-black text-white"><Plus className="size-3.5"/> افزودن</button></form>}
